<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MD Shahriar Alam Sifat | News</title>
  <style>
    :root { --bg-color: #fafafa; --card-bg: #ffffff; --text-main: #0f172a; --text-muted: #64748b; --accent: #2563eb; --accent-light: #eff6ff; --border: #e2e8f0; }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif; background: var(--bg-color); color: var(--text-main); line-height: 1.7; }
    
    .navbar { position: sticky; top: 0; background: rgba(255, 255, 255, 0.9); backdrop-filter: blur(10px); z-index: 1000; padding: 1.2rem 2rem; border-bottom: 1px solid var(--border); display: flex; justify-content: center; gap: 2.5rem; }
    .navbar a { text-decoration: none; color: var(--text-main); font-weight: 600; font-size: 0.95rem; transition: color 0.2s; text-transform: uppercase; letter-spacing: 0.05em; }
    .navbar a:hover { color: var(--accent); }
    .navbar a.active { color: var(--accent); border-bottom: 2px solid var(--accent); padding-bottom: 2px; }

    .container { max-width: 1050px; margin: 3rem auto; padding: 0 1.5rem; }
    .header-card { background: var(--card-bg); border: 1px solid var(--border); border-radius: 16px; padding: 2.5rem; }
    
    h1 { font-size: 2.2rem; margin-bottom: 0.5rem; font-weight: 700; }
    p.subtitle { color: var(--text-muted); font-size: 1rem; margin-bottom: 2.5rem; border-bottom: 1px solid var(--border); padding-bottom: 1.5rem; }

    .timeline-item { margin-bottom: 2rem; padding-bottom: 2rem; border-bottom: 1px dashed var(--border); display: flex; gap: 1.5rem;}
    .timeline-item:last-child { border-bottom: none; margin-bottom: 0; padding-bottom: 0; }
    
    .event-tag { min-width: 100px; padding: 0.4rem 0.8rem; background: var(--accent-light); color: var(--accent); border-radius: 8px; font-weight: 600; font-size: 0.75rem; text-transform: uppercase; text-align: center; height: fit-content; letter-spacing: 0.05em;}
    .event-content h3 { font-size: 1.25rem; color: var(--text-main); margin-bottom: 0.5rem; font-weight: 600;}
    .event-content p { color: var(--text-muted); font-size: 0.95rem; line-height: 1.6; }

    @media (max-width: 768px) { 
      /* Mobile Navbar Fixes */
      .navbar { padding: 1rem 0.5rem; gap: 0.8rem; flex-wrap: wrap; justify-content: center; } 
      .navbar a { font-size: 0.8rem; letter-spacing: 0.02em; }
      
      /* Mobile Layout Fixes */
      .timeline-item { flex-direction: column; gap: 0.5rem; }
      .event-tag { max-width: 120px; text-align: left; background: transparent; padding: 0; margin-bottom: -0.5rem;}
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <a href="index.html">Blog</a>
    <a href="news.html" class="active">News</a>
    <a href="about.html">About Me</a>
  </nav>

  <div class="container">
    <div class="header-card">
      <h1>FinTech Market News</h1>
      <p class="subtitle">Monitoring the local financial ecosystem, digital integration, and technology trends.</p>
      
      <!-- Timeline -->
      <div class="timeline">
        
        <div class="timeline-item">
          <div class="event-tag">Infrastructure</div>
          <div class="event-content">
            <h3>Tracking Digital Payment Frameworks</h3>
            <p>Monitoring the integration of interoperable frameworks like Bangla QR and Mojaloop within the domestic banking sector, observing how these technologies impact retail liquidity and SME transactions.</p>
          </div>
        </div>

      </div>
    </div>
  </div>
</body>
</html>
